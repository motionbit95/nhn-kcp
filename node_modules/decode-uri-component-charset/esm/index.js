import iconv from 'iconv-lite';
export var decodeURIComponentCharset = function (str, charset) {
    if (!str) {
        return '';
    }
    return str.replace(/(%([^%]{2}))+/g, function (chars) { return iconv.decode(Buffer.from(chars.split('%').slice(1).map(function (n) { return parseInt('0x' + n); })), charset); });
};
